import 'package:flutter/material.dart';

// Define the colors
const secondaryColor = Color(0xFFED7F10); // Blue color with full opacity
const primaryColor = Color(0xFF0080FF); // Example orange color
const bgColor = Color(0xFF212332); // Background color

const defaultPadding = 16.0;